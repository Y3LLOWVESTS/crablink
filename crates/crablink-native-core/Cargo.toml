[package]
name = "crablink-native-core"
version = "0.1.0"
description = "Pure Rust validation core shared by CrabLink native clients."
authors = ["RustyOnions contributors"]
license = "MIT OR Apache-2.0"
edition = "2021"
rust-version = "1.80"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
